import React, { useContext } from 'react'
import { DataContext } from '../../context/DataContext'

const DashBoard = () => {
    const {doctor}=useContext(DataContext);
  return (
    <div>
      
    </div>
  )
}

export default DashBoard
